// src/App.jsx
import { AppProvider } from "./context/AppContext";


export default function App() {
  return (
    <AppProvider>
      <main>
        {/* Preloader — next step */}
      </main>
    </AppProvider>
  )
}